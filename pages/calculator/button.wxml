<template name="button">
  <view class="button{{long ? ' long' : ''}}{{color == 'gray' ? ' gray' : ''}}{{color == 'orange' ? ' orange' : ''}}">
    <view class="text">{{text}}</view>
  </view>
</template>
